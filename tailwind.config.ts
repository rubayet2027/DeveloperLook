// Tailwind CSS v4 — config is handled entirely in globals.css via @theme
// This file intentionally left minimal; v4 does not need theme.extend here
import type { Config } from 'tailwindcss'
const config: Config = {}
export default config
